name: Build FastAPI Demo Image

on:
  push:
    branches:
      - main
    paths:
      - "applications/workloads/fastapi-demo/**"
      - ".github/workflows/fastapi-demo-image.yaml"

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: applications/workloads/fastapi-demo
          push: true
          tags: |
            ghcr.io/axelsson2000/onc-fastapi-demo:main
            ghcr.io/axelsson2000/onc-fastapi-demo:${{ github.sha }}
